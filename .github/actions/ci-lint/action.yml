# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  🔍 Composite Action: ci-lint                                              ║
# ║                                                                              ║
# ║  ESLint + TypeScript type check (tsc --noEmit).                            ║
# ║  Inclui checkout e setup internamente — job chamador não precisa de steps. ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

name: 'CI — Lint & Type Check'
description: 'ESLint + tsc --noEmit. Executa checkout e setup internamente.'

inputs:
  node-version:
    description: 'Versão do Node.js'
    required: false
    default: '22.x'

runs:
  using: composite
  steps:
    - name: ⬇️ Checkout
      uses: actions/checkout@v6

    - name: 🟢 Setup Node.js ${{ inputs.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        cache: 'npm'

    - name: 📥 Instalar dependências
      shell: bash
      run: npm ci --prefer-offline

    - name: 🔍 ESLint
      id: eslint
      shell: bash
      run: npm run lint

    - name: 🏷️ TypeScript type check
      id: typecheck
      shell: bash
      run: npx tsc --noEmit

    - name: 📊 Sumário — Lint & Type Check
      if: always()
      shell: bash
      run: |
        {
          echo "## 🔍 Lint & Type Check"
          echo ""
          echo "| Check | Status |"
          echo "|-------|--------|"
          echo "| 🔍 ESLint     | $([[ '${{ steps.eslint.outcome }}' == 'success' ]] && echo '✅ Passou' || echo '❌ Falhou') |"
          echo "| 🏷️ TypeScript | $([[ '${{ steps.typecheck.outcome }}' == 'success' ]] && echo '✅ Passou' || echo '❌ Falhou') |"
        } >> "$GITHUB_STEP_SUMMARY"
